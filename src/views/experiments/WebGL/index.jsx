import React, { PureComponent } from 'react';
import PropTypes from 'prop-types';

class WebGL extends PureComponent {
    render() {
        return (
            <div>WebGL Example here</div>
        );
    }
}
export default WebGL;